'use client';

import { ProtectedRoute } from '@/components/protected-route';

export default function FinancingSupportPage() {
  return (
    <ProtectedRoute>
      <div className="flex-1 bg-white h-full">
        {/* Blank page - Financing Support */}
      </div>
    </ProtectedRoute>
  );
}

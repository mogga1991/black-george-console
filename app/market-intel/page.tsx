'use client';

import { ProtectedRoute } from '@/components/protected-route';

export default function MarketIntelPage() {
  return (
    <ProtectedRoute>
      <div className="flex-1 bg-white h-full">
        {/* Blank page - Market Intel */}
      </div>
    </ProtectedRoute>
  );
}

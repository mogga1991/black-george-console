'use client';

import { ProtectedRoute } from '@/components/protected-route';

export default function SolicitationMapPage() {
  return (
    <ProtectedRoute>
      <div className="flex-1 bg-white h-full">
        {/* Blank page - Solicitation Map */}
      </div>
    </ProtectedRoute>
  );
}